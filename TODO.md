# TODO - Run Real-Time-Website-Threat-Detection

- [x] Fix dependency install failure in `.venv` (`ipython==8.23.0` not available for this environment)
- [x] Re-run `pip install -r requirements.txt` in the venv
- [x] Start the Flask app (`python app.py`)
- [x] Verify the server is reachable at `http://127.0.0.1:5000`
- [ ] Run a quick test POST/GET with a sample URL to confirm model prediction + template rendering


